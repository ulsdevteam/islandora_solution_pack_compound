// Parent manage tab if perm
// Active image thumb
// If has permission, link to parent's metadata tab
// Click on object in tree, ajax to get first level children
// Dedicated compound object form
// In parent object tab, link to create child object
// Thumbs navigation?
// Default sort of siblings? configurable?
// Cache children of compound object?
// Optional block with compound navigation
// Theme parent metadata
